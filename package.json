{
  "name": "sistema-pet-root",
  "version": "1.0.0",
  "description": "Root proxy package for Render deployment",
  "main": "backend/src/server.js",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node src/server.js"
  }
}
